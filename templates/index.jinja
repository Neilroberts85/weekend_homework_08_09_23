<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>

<form action="/books" method="post">
    <h1>add book</h1>
    <label for ="title">Title</label>
    <input type="text" name ="title" id="title">
    <br>

    <label for ="author">Author</label>
    <input type ="text" name="author" id="author">
    <br>
    <label for ="genre">Genre</label>
    <input type ="text" name="genre" id="genre">

    <label for ="true">Checked out</label>
    <input type="radio" name="checked_out" id="true" value="true">

    <label for ="false">Checked in</label>
    <input type="radio" name="checked_out" id="false" value="false">

    <input type ="submit" value="Add Book">
</form>

<h1>{{ title }}</h1>
{% for book in books %}
    <h1>{{ book.title }}</h1>
    <h2>by {{ book.author}}</h2>
    <h5>{{ book.genre}}</h5>
    <h3>{% if book.checked_out%}
                checked out
            {%else%}
                checked in
        {%endif%}
    </h3>
    <form class="form-inline" action="/books/delete/{{ book.title }}" method="POST">
            <input type="submit" value="Delete">
    </form>
{% endfor %}
    
</body>
</html>




